# Source build. Layer over the base file:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d
# The result is tagged kybookmarks-server:local so it never occupies the published name.
services:
  kybookmarks-server:
    image: kybookmarks-server:local
    pull_policy: build
    build:
      context: .
      dockerfile: Dockerfile
